@import '../../styl/variable.styl'
.wrapper
  display flex
  justify-content center
  a
    height h=30px
    width h
    text-align center
    i
      height h
      width h
      font-size 30px
      color $medium
      transition transform ease-in-out 0.2s
      &:hover
        transform scale(1.2)
